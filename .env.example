# Symfony Environment Configuration
# Copy this file to .env for local development

# Application environment (dev, prod, test)
APP_ENV=dev

# Application secret key (used for CSRF protection, encryption, etc.)
# Generate a new one using: php -r "echo bin2hex(random_bytes(16));"
APP_SECRET=1234567890abcdefghijklmnopqrstuvwxyz

# Debug mode (true for development, false for production)
APP_DEBUG=true
